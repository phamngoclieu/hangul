{
  "name": "sejong-green",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "validate": "node scripts/validate-content.mjs",
    "build": "node scripts/validate-content.mjs && vite build",
    "preview": "vite preview",
    "publish:root": "node scripts/publish-root.mjs",
    "release": "node scripts/validate-content.mjs && vite build && node scripts/publish-root.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.111.0"
  },
  "devDependencies": {
    "vite": "^8.1.5"
  }
}
